from setuptools import setup, find_packages

setup(
    name='jetson-hardware',
    version='0.1.0',
    packages=find_packages(where='.'),
    description='Jetson Orin Nano hardware management modules',
    author='Your Name',
    install_requires=[],
    python_requires='>=3.7',
)
